[deploy]
# Run database migrations before starting the app
# This executes INSIDE Railway's private network with access to internal URLs
# Only used for PRODUCTION (Railway Postgres) - Staging uses GitHub Actions (Supabase)
preDeployCommand = "pnpm db:migrate"
